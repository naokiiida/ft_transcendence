# 構造理解

## ルート構成

- `frontend/` Next.js（フロントエンド）
- `backend/` NestJS（バックエンド API）
- `infra/` 監視系（Prometheus / Grafana）
- `specs/` 仕様・設計メモ
- `docker-compose.yml` ルートで統合起動
- `CLAUDE.md`/`個人メモリスト.md` など補助ドキュメント

## フロントエンド（frontend）

- `app/` App Router エントリ（`layout.tsx`, `page.tsx`）
- `components/` UI コンポーネント群（game/shared/ui）
- `public/` 静的アセット
- `lib/` ユーティリティ
- 追加済み API ルート: `/api/health`, `/api/metrics`
- Docker 実行は 3000 番ポート

## バックエンド（backend）

- NestJS 初期構成（`src/`, `test/`）
- ルート `/` の 基本エンドポイントのみ（現状はテンプレ）
- Docker 実行は 3000 番ポート（ホストは 3001）

## インフラ（infra）

- `prometheus/` スクレイプ設定
- `grafana/` プロビジョニング/ダッシュボード
- `traefik/` は未配置（必要なら追加）

## 起動ポート（docker-compose）

- frontend: `http://localhost:3000`
- backend: `http://localhost:3001`
- grafana: `http://localhost:3002`
- prometheus: `http://localhost:9090`
