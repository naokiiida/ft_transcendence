・難易度ごとの CPU の強さ調整をする。

とりあえず nestJS でテスト
（nextJS で完結する場合も両睨みでテストをする。）
ローカルゲームテスト（AI）
リモートマッチ考慮すると WebSocket とゲーム理解
SPA
pong が面白くない、バーの場所で少しエッジを効かせるか。
ボール追従の扱いは、以前は駄目だった？

・engin.ts の描画を分離？
・古い updategame()、getCpuinput
・AIcontroller の humancontroller 型が緩い
・spawnBall
・衝突判定がめり込み

## SPA にする場合の方針メモ

### 1) Next.js のまま「CSR 寄り」で運用する場合

- すべてクライアントコンポーネント化（各ページに `"use client"`）
- Server Components / Server Actions を使わない
- 画面遷移は `next/link` を使用
- 必要なら `next/dynamic({ ssr: false })` で完全 CSR に寄せる
- `app/api/*` は使えるが、SPA というより「CSR 中心の Next.js」になる

### 2) 完全 SPA（静的ホスティング前提）にする場合

- `frontend/next.config.ts` で `output: "export"` を設定
- `app/api/*` を削除 or 別 API（NestJS など）に移行
- すべてクライアントで動くようにする
- ビルド成果物は静的配信

## 公開前チェック

Gradana admin/admin
traefik --api.insecure=true
Prometheus/Grafana 認証なし
Next.js コンテナ非ルートユーザー
ポート 3000、3001、3002 はこっちの環境想定なので、修正
