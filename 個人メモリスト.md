npm 想定でいく。
とりあえず nestJS でテスト
ローカルゲームテスト（AI）
リモートマッチ考慮すると WebSocket とゲーム理解
SPA

## SPA にする場合の方針メモ

### 1) Next.js のまま「CSR寄り」で運用する場合
- すべてクライアントコンポーネント化（各ページに `"use client"`）
- Server Components / Server Actions を使わない
- 画面遷移は `next/link` を使用
- 必要なら `next/dynamic({ ssr: false })` で完全CSRに寄せる
- `app/api/*` は使えるが、SPAというより「CSR中心のNext.js」になる

### 2) 完全SPA（静的ホスティング前提）にする場合
- `frontend/next.config.ts` で `output: "export"` を設定
- `app/api/*` を削除 or 別API（NestJSなど）に移行
- すべてクライアントで動くようにする
- ビルド成果物は静的配信

## 公開前チェック

.env を ignore に復帰
Gradana admin/admin
traefik --api.insecure=true
Prometheus/Grafana 認証なし
Next.js コンテナ非ルートユーザー
ポート 3000、3001、3002 はこっちの環境想定なので、修正
