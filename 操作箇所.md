# 操作箇所

## 未操作の改善箇所（要対応）

- Traefik の設定ディレクトリが未配置

  - `./infra/traefik` が存在しないため production profile で起動すると失敗
  - 該当: `ft_transcendence/docker-compose.yml`

- 環境変数 `DENO_ENV` の扱い
  - Next/Nest では未使用のため整理が必要
  - 該当: `ft_transcendence/.env`, `ft_transcendence/docker-compose.yml`

## 操作済み

- フロントの待受ポートを `3000` に統一

  - compose と Traefik の参照を 3000 に修正
  - Prometheus の scrape target を `frontend:3000` に修正

- Healthcheck / Metrics のエンドポイント追加

  - `/api/health` を追加（healthcheck 用）
  - `/api/metrics` を追加（Prometheus 用の簡易メトリクス）

- Storybook の story 名衝突を修正

  - `Promise` -> `PromiseToast`

- Grafana のホスト側ポートを `3002` に変更

  - `3000/3001` 競合回避

- Traefik 設定ディレクトリを追加
  - `infra/traefik/` を作成し、空マウント失敗を回避

## 操作したファイル

- `ft_transcendence/docker-compose.yml`
- `ft_transcendence/infra/prometheus/prometheus.yml`
- `ft_transcendence/frontend/app/api/health/route.ts`
- `ft_transcendence/frontend/app/api/metrics/route.ts`
- `ft_transcendence/frontend/components/ui/sonner.stories.tsx`
- `ft_transcendence/infra/traefik/README.md`
