# Hurl test variables
# Usage: hurl --variables-file test/hurl/vars.env test/hurl/*.hurl
#
# Generate unique timestamp for test runs to avoid conflicts
# In CI, this can be overridden with: hurl --variable timestamp=$(date +%s)

timestamp=test_run
